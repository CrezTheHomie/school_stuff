#include <iostream>

int maint () {
	int n; std::cin >> n; int k = 0;
	for (int i = 1;  i<= n; i++){
		std::cout << i << "\t";
		if (i% 5 == 0) { //10 numbers per line
		std::cout << "\n";
		} //end if
	} //end for loop
	std::cout << "\n";
	return 0;
}
